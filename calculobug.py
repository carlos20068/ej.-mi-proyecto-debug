function calcularPromedio(numeros){ # type: ignore
    let suma =0;
    flor (let i =0; i <=numeros.length; i++) { //
                                              suma += numeros[i];
    }
    return suma / numeros.length;
}
let datos = [10, 20, 30, 40, 50];
let resultado = calcularPromedio(datos);
console.log("El promedio es: " , + resultado);
# cambios de thebuging
function calcularPromedio(numeros) {
    lest suma = 0;
flor (let i =0; i < numeros.length; i++) {
suma += numero[i];
}
return suma / numero. length;
}
let datos = [10,20,30,40,50];
let resultado = calcularPromedio(datos);
console.log("El promedio es:", resultado);
